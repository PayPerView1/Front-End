"use client";

import PageLoader from "@/components/PageLoader";

/**
 * DeletingState
 * يُعرض عند الضغط على "حذف نهائي" — نفس لودينج تسجيل الدخول والخروج
 */
export default function DeletingState() {
  return <PageLoader />;
}
