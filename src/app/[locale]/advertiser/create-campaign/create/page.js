import CreateCampaignClient from "./components/CreateCampaignClient";

export const metadata = {
  title: "Create Campaign | Pay Per View",
};

export default function CreateCampaignPage() {
  return <CreateCampaignClient />;
}
