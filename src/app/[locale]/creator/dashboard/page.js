import ProfileContent from "@/app/[locale]/creator/dashboard/components/ProfileContent";

export const metadata = {
  title: "الملف الشخصي | Pay Per View",
};

export default function DashboardPage() {
  return <ProfileContent />;
}